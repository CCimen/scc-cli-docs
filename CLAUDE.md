# CLAUDE.md - SCC Docs

Read `AGENTS.md` first. This repo documents the SCC CLI in the sibling `../scc` source repo.

Use `bun run astro check` for validation after content, config, or component changes. Keep security and runtime claims aligned with the source implementation and the SCC constitution in `../scc/CONSTITUTION.md`.
