---
title: Quick Start
description: Get running with SCC CLI in 30 seconds
sidebar:
  order: 3
  badge:
    text: Start
    variant: success
---

import { Steps, Tabs, TabItem, Aside, Card, CardGrid } from '@astrojs/starlight/components';

<Aside>
  **Prerequisites:** Python 3.10+, Docker Desktop 4.50+, Git 2.30+. See [Installation](/getting-started/installation/) for details.
</Aside>

## 30-Second Setup

<Steps>
1. **Install SCC CLI**

   <Tabs syncKey="pkg">
     <TabItem label="uv (recommended)">
     ```bash
     uv tool install scc-cli
     ```
     </TabItem>
     <TabItem label="pipx">
     ```bash
     pipx install scc-cli
     ```
     </TabItem>
     <TabItem label="pip">
     ```bash
     pip install scc-cli
     ```
     </TabItem>
   </Tabs>

2. **Configure your organization** (if you have an org config URL)

   ```bash
   scc setup
   ```

   When prompted, paste your organization's config URL. If you don't have one, use `--standalone` mode:

   ```bash
   scc setup --standalone
   ```

3. **Launch Claude Code in your project**

   ```bash
   cd ~/your-project
   scc
   ```

   Or specify the path directly:

   ```bash
   scc start ~/your-project
   ```
</Steps>

<Aside type="tip">
  If you install plugins inside the container, run `scc profile save` afterward to capture your personal setup. SCC will detect sandbox-only plugin changes and can import them into `.claude/settings.local.json` before saving.
</Aside>

## What Happens When You Run `scc`

1. **If no org config exists**, SCC asks whether to run setup or standalone mode
2. **Auto-detects workspace** from git repository root or `.scc.yaml` location
3. **If no workspace is detected**, SCC opens the Dashboard so you can start a session
4. **Shows Quick Resume** (always includes “New session” as the default)
5. **Prints brief context** (workspace root, entry directory, team) before launching
6. **Starts Docker container** with your team's configuration

<Aside type="tip">
  **Safety guard:** SCC won't auto-launch from suspicious directories (home, `/tmp`). If you try `scc start ~/`, it will prompt for confirmation.
</Aside>

## Keyboard Shortcuts

When in the dashboard:

| Key | Action |
|-----|--------|
| `↑↓` | Navigate list |
| `Enter` | Open action menu (containers/sessions/worktrees) |
| `Tab` | Switch between tabs |
| `n` | Start new session |
| `t` | Switch team |
| `r` | Refresh |
| `s` | Settings & Maintenance |
| `?` | Help |
| `q` | Quit |

## Verify Your Setup

Run the diagnostic command to ensure everything is configured correctly:

```bash
scc doctor
```

Expected output:
```
✓ Docker: OK (version 27.0.0)
✓ Git: OK (version 2.45.0)
✓ Python: OK (version 3.12.0)
✓ Config: OK
✓ Organization: Connected to "My Org"
✓ Team: backend
```

## Common First-Time Issues

| Problem | Solution |
|---------|----------|
| "Cannot connect to Docker" | Start Docker Desktop and wait for it to initialize |
| "Docker version too old" | Update to Docker Desktop 4.50+ |
| "Organization config fetch failed" | Check your org URL is correct and accessible |

See [Troubleshooting](/troubleshooting/) for more solutions.

## Next Steps

<CardGrid>
  <Card title="Core Concepts" icon="open-book">
    Understand sessions, worktrees, and profiles. [Learn more →](/getting-started/core-concepts/)
  </Card>
  <Card title="Personal Profiles" icon="user">
    Save your personal setup per project and sync across machines. [Read guide →](/guides/developer/personal-profiles/)
  </Card>
  <Card title="Developer Onboarding" icon="rocket">
    Joining a team? See what you get automatically. [Read guide →](/guides/developer/onboarding/)
  </Card>
  <Card title="Team Setup" icon="setting">
    Managing a team? Configure plugins and profiles. [Set up →](/guides/team-leader/overview/)
  </Card>
</CardGrid>
